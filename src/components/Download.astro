<section class="download" id="muat-turun">
	<div class="download__inner">
		<p class="download__kicker">Akses MARC</p>
		<h2>Portal web tersedia sekarang</h2>
		<p>
			Gunakan portal web untuk pendaftaran, aktiviti, notifikasi dan urusan
			keahlian. Aplikasi mudah alih sedang disediakan.
		</p>
		<p class="download__note">
			Sudah ahli kelab? Hubungi pihak pengurusan MARC untuk maklumat akses.
		</p>
	</div>
</section>

<style>
	.download {
		padding: 72px 24px;
		background: var(--color-secondary-dark);
		color: white;
		border-top: 1px solid color-mix(in srgb, white 12%, transparent);
	}

	.download__inner {
		max-width: 640px;
		margin: 0 auto;
	}

	.download__kicker {
		margin-bottom: 10px;
		color: color-mix(in srgb, white 68%, transparent);
		font-size: 0.78rem;
		font-weight: 600;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	h2 {
		font-size: clamp(1.6rem, 3vw, 2.2rem);
		color: white;
		margin-bottom: 16px;
	}

	.download__inner > p {
		font-size: 1.02rem;
		line-height: 1.6;
		color: color-mix(in srgb, white 88%, transparent);
	}

	.download__note {
		margin-top: 28px;
		font-size: 0.92rem;
		opacity: 0.85;
	}
</style>

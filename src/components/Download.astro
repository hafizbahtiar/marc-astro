<section class="download" id="muat-turun">
	<div class="download__inner">
		<h2>Mulakan dengan MARC</h2>
		<p>
			Portal web MARC sudah tersedia untuk pendaftaran, aktiviti, notifikasi
			dan urusan keahlian. Aplikasi mudah alih akan menyusul selepas semakan
			store selesai.
		</p>
		<a href="/login" class="download__button">Buka portal web</a>
		<div class="download__badges">
			<span class="badge">
				<span class="badge__label">Akan Datang di</span>
				<span class="badge__store">App Store</span>
			</span>
			<span class="badge">
				<span class="badge__label">Akan Datang di</span>
				<span class="badge__store">Google Play</span>
			</span>
		</div>
		<p class="download__note">
			Sudah ahli kelab? Hubungi pihak pengurusan MARC untuk maklumat
			akses.
		</p>
	</div>
</section>

<style>
	.download {
		padding: 72px 24px;
		background: var(--color-primary);
		color: white;
		text-align: center;
	}

	.download__inner {
		max-width: 640px;
		margin: 0 auto;
	}

	h2 {
		font-size: clamp(1.6rem, 3vw, 2.2rem);
		color: white;
		margin-bottom: 16px;
	}

	.download__inner > p {
		font-size: 1.02rem;
		line-height: 1.6;
		color: color-mix(in srgb, white 88%, transparent);
	}

	.download__badges {
		margin-top: 32px;
		display: flex;
		justify-content: center;
		gap: 16px;
		flex-wrap: wrap;
	}

	.download__button {
		display: inline-flex;
		margin-top: 28px;
		padding: 13px 22px;
		border: 1px solid color-mix(in srgb, white 45%, transparent);
		border-radius: 10px;
		color: white;
		font-weight: 600;
		text-decoration: none;
		transition: background 0.15s ease;
	}

	.download__button:hover {
		background: color-mix(in srgb, white 12%, transparent);
	}

	.badge {
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 2px;
		padding: 10px 20px;
		border-radius: 12px;
		border: 1.5px solid color-mix(in srgb, white 35%, transparent);
		min-width: 168px;
		opacity: 0.85;
	}

	.badge__label {
		font-size: 0.7rem;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		opacity: 0.8;
	}

	.badge__store {
		font-size: 1.05rem;
		font-weight: 600;
	}

	.download__note {
		margin-top: 28px;
		font-size: 0.92rem;
		opacity: 0.85;
	}
</style>
